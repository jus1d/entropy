version: "2"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign
    - revive
    - gocritic

  settings:
    revive:
      rules:
        - name: exported
          disabled: true

run:
  timeout: 5m
